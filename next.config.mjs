/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['wallpapers.com'], // Add wallpapers.com as an allowed image domain
    },
};

export default nextConfig;
